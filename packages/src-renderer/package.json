{
  "name": "renderer",
  "version": "0.1.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint src/**/*.{js,ts}"
  },
  "devDependencies": {
    "vite": "^2.6.13"
  }
}
